[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-mcp-bridge"
version = "0.1.0"
description = "MCP bridge — Model Context Protocol integration"
license = {text = "MIT"}
requires-python = ">=3.8"
